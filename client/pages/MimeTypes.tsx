"use client";
import { useMemo, useState } from "react";
import { Copy } from "lucide-react";
import SEO from "@/components/SEO";
import { mimeTypes } from "@/lib/web-sec-3";
import { useTranslations } from "next-intl";
export default function MimeTypes() { const t = useTranslations("Tools.MimeTypes"); const [search, setSearch] = useState(""); const results = useMemo(() => mimeTypes.filter(item => item.extension.toLowerCase().includes(search.toLowerCase().replace(/^\./, "")) || item.mime.toLowerCase().includes(search.toLowerCase())), [search]); const copy = (value: string) => void navigator.clipboard.writeText(value); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="MIME type extension directory lookup" /><div className="mx-auto max-w-5xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold lg:text-5xl">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-6"><input value={search} onChange={e => setSearch(e.target.value)} placeholder={t("searchPlaceholder")} className="w-full rounded-xl border border-[#E0E0E0] bg-white px-4 py-3 text-[#2D4D35]" /><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[560px] text-left"><thead><tr className="border-b border-[#E0E0E0] text-sm text-[#4A6857]"><th className="p-4">{t("extension")}</th><th className="p-4">{t("mime")}</th><th className="p-4">{t("actions")}</th></tr></thead><tbody>{results.map(item => <tr className="border-b border-[#E0E0E0] text-sm text-[#2D4D35]" key={`${item.extension}-${item.mime}`}><td className="p-4 font-mono font-bold">.{item.extension}</td><td className="p-4 font-mono">{item.mime}</td><td className="p-4"><div className="flex gap-2"><button onClick={() => copy(item.extension)} className="rounded-lg border border-[#E0E0E0] px-3 py-1">{t("copyExtension")}</button><button onClick={() => copy(item.mime)} className="inline-flex items-center gap-1 rounded-lg border border-[#E0E0E0] px-3 py-1"><Copy className="h-3 w-3" />{t("copyMime")}</button></div></td></tr>)}</tbody></table></div></section></div></main>; }
