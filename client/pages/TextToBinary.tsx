"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { binaryToText, textToBinary } from "@/lib/converter-tools-2";
import { useTranslations } from "next-intl";
export default function TextToBinary() { const t = useTranslations("Tools.TextToBinary"); const [text, setText] = useState(""); const [binary, setBinary] = useState(""); const [bits, setBits] = useState(8); const encoded = useMemo(() => textToBinary(text, bits), [bits, text]); const decoded = useMemo(() => { try { return binaryToText(binary, bits); } catch { return ""; } }, [binary, bits]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="text to ASCII binary converter" /><div className="mx-auto max-w-4xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark mb-6 rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("bitWidth")}<select value={bits} onChange={e => setBits(Number(e.target.value))} className="ml-3 rounded-lg border border-[#E0E0E0] bg-white px-3 py-2"><option value={8}>8-bit</option><option value={16}>16-bit</option></select></label></section><div className="grid gap-6 lg:grid-cols-2">{[[t("textToBinary"), text, setText, encoded, t("textInput"), t("binaryOutput")], [t("binaryToText"), binary, setBinary, decoded, t("binaryInput"), t("textOutput")]].map(([heading, input, setter, output, inputLabel, outputLabel]) => <section className="glass-card-dark rounded-[24px] p-8" key={heading as string}><h2 className="mb-5 text-xl font-bold text-[#2D4D35]">{heading as string}</h2><label className="block text-sm font-semibold text-[#2D4D35]">{inputLabel as string}<textarea value={input as string} onChange={e => (setter as (value: string) => void)(e.target.value)} rows={6} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 font-mono text-sm" /></label><label className="mt-5 block text-sm font-semibold text-[#2D4D35]">{outputLabel as string}<textarea readOnly value={output as string} rows={6} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 font-mono text-sm" /></label></section>)}</div></div></main>; }
