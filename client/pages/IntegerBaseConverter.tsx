"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { convertIntegerBase } from "@/lib/converter-tools-2";
import { useTranslations } from "next-intl";
export default function IntegerBaseConverter() { const t = useTranslations("Tools.IntegerBaseConverter"); const [input, setInput] = useState("42"); const [from, setFrom] = useState(10); const [custom, setCustom] = useState(36); const converted = (base: number) => { try { return convertIntegerBase(input, from, base); } catch { return ""; } }; const error = useMemo(() => { try { convertIntegerBase(input, from, custom); return ""; } catch { return t("invalid"); } }, [custom, from, input, t]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="integer base converter binary hexadecimal" /><div className="mx-auto max-w-4xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("input")}<input value={input} onChange={e => setInput(e.target.value)} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2 font-mono" /></label><label className="mt-4 block text-sm font-semibold text-[#2D4D35]">{t("inputBase")}<input type="number" min={2} max={36} value={from} onChange={e => setFrom(Math.max(2, Math.min(36, Number(e.target.value))))} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2" /></label>{error && <p className="mt-3 text-sm text-red-600">{error}</p>}<div className="mt-6 space-y-3">{[[2, t("binary")], [8, t("octal")], [10, t("decimal")], [16, t("hexadecimal")]].map(([base, label]) => <div className="flex items-center gap-3" key={base as number}><span className="w-28 font-semibold text-[#2D4D35]">{label as string}</span><input readOnly value={converted(base as number)} className="flex-1 rounded-lg border border-[#E0E0E0] bg-white px-3 py-2 font-mono text-sm" /></div>)}<div className="flex items-center gap-3"><label className="w-28 font-semibold text-[#2D4D35]">{t("custom")}</label><input type="number" min={2} max={36} value={custom} onChange={e => setCustom(Math.max(2, Math.min(36, Number(e.target.value))))} className="w-20 rounded-lg border border-[#E0E0E0] bg-white px-2 py-2" /><input readOnly value={converted(custom)} className="flex-1 rounded-lg border border-[#E0E0E0] bg-white px-3 py-2 font-mono text-sm" /></div></div></section></div></main>; }
