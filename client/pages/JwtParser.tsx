"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { parseJwt } from "@/lib/web-text-tools";
import { useTranslations } from "next-intl";
const sample = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c";
export default function JwtParser() { const t = useTranslations("Tools.JwtParser"); const [token, setToken] = useState(sample); const result = useMemo(() => parseJwt(token), [token]); const section = (title: string, rows: ReturnType<typeof parseJwt>["header"]) => <section className="glass-card-dark rounded-[24px] p-6"><h2 className="mb-4 text-xl font-bold text-[#2D4D35]">{title}</h2>{rows.length ? <div className="space-y-3">{rows.map(row => <div className="grid gap-2 border-b border-[#E0E0E0] pb-3 sm:grid-cols-[180px_1fr]" key={`${row.claim}-${row.value}`}><div className="font-semibold text-[#2D4D35]">{row.claim}{row.description && <span className="ml-2 text-xs font-normal text-[#4A6857]">({row.description})</span>}</div><div className="break-words font-mono text-sm text-[#2D4D35]">{row.value}{row.friendlyValue && <span className="ml-2 font-sans text-xs text-[#4A6857]">({row.friendlyValue})</span>}</div></div>)}</div> : <p className="text-sm text-red-600">{result.error || t("invalid")}</p>}</section>; return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="JWT parser decoder" /><div className="mx-auto max-w-5xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark mb-6 rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("inputLabel")}<textarea value={token} onChange={e => setToken(e.target.value)} rows={5} placeholder={t("placeholder")} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 font-mono text-sm text-[#2D4D35]" /></label>{result.valid && <p className={`mt-3 text-sm ${result.expired ? "text-red-600" : "text-[#10A968]"}`}>{result.expired ? t("expired") : t("valid")}</p>}</section>{result.valid && <div className="grid gap-6 lg:grid-cols-2">{section(t("header"), result.header)}{section(t("payload"), result.payload)}</div>}</div></main>; }
