"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { convertCases } from "@/lib/converter-tools";
import { useTranslations } from "next-intl";
export default function CaseConverter() { const t = useTranslations("Tools.CaseConverter"); const [input, setInput] = useState("lorem ipsum dolor sit amet"); const formats = useMemo(() => convertCases(input), [input]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="case converter camelCase snake_case kebab-case" /><div className="mx-auto max-w-4xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("inputLabel")}<textarea value={input} onChange={e => setInput(e.target.value)} rows={4} placeholder={t("placeholder")} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 text-[#2D4D35]" /></label><div className="mt-6 space-y-3">{Object.entries(formats).map(([key, value]) => <div className="flex items-center gap-3" key={key}><span className="w-32 shrink-0 text-sm font-semibold capitalize text-[#4A6857]">{key}</span><input readOnly value={value} className="min-w-0 flex-1 rounded-lg border border-[#E0E0E0] bg-white px-3 py-2 font-mono text-sm text-[#2D4D35]" /><button onClick={() => void navigator.clipboard.writeText(value)} className="rounded-lg bg-[#E8F0E8] px-3 py-2 text-sm text-[#2D4D35]">{t("copy")}</button></div>)}</div></section></div></main>; }
