"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { svgDataUri, svgPlaceholder } from "@/lib/media-visual-tools";
import { useTranslations } from "next-intl";
import FaqSection from "@/components/FaqSection";
import { getFaqsForRoute } from "@/lib/faq-data";
export default function SvgPlaceholderGenerator() { const t = useTranslations("Tools.SvgPlaceholderGenerator");
  const faqs = getFaqsForRoute("unknown-slug"); const [width, setWidth] = useState(600); const [height, setHeight] = useState(350); const [fontSize, setFontSize] = useState(26); const [background, setBackground] = useState("#cccccc"); const [foreground, setForeground] = useState("#333333"); const [text, setText] = useState(""); const [exact, setExact] = useState(true); const svg = useMemo(() => svgPlaceholder(width, height, fontSize, background, foreground, text, exact), [background, exact, fontSize, foreground, height, text, width]); const dataUri = useMemo(() => svgDataUri(svg), [svg]); const copy = (value: string) => void navigator.clipboard.writeText(value); const download = () => { const link = document.createElement("a"); link.href = dataUri; link.download = "placeholder.svg"; link.click(); }; return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="SVG placeholder generator" /><div className="mx-auto max-w-5xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold text-[#2D4D35]">{t("width")}<input type="number" min={1} value={width} onChange={e => setWidth(Math.max(1, Number(e.target.value)))} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2" /></label><label className="text-sm font-semibold text-[#2D4D35]">{t("height")}<input type="number" min={1} value={height} onChange={e => setHeight(Math.max(1, Number(e.target.value)))} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2" /></label><label className="text-sm font-semibold text-[#2D4D35]">{t("fontSize")}<input type="number" min={1} value={fontSize} onChange={e => setFontSize(Math.max(1, Number(e.target.value)))} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2" /></label></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm text-[#4A6857]">{t("background")}<input type="color" value={background} onChange={e => setBackground(e.target.value)} className="mt-2 h-10 w-full" /></label><label className="text-sm text-[#4A6857]">{t("foreground")}<input type="color" value={foreground} onChange={e => setForeground(e.target.value)} className="mt-2 h-10 w-full" /></label></div><label className="mt-4 block text-sm font-semibold text-[#2D4D35]">{t("customText")}<input value={text} onChange={e => setText(e.target.value)} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2" /></label><label className="mt-4 flex items-center gap-2 text-sm text-[#4A6857]"><input type="checkbox" checked={exact} onChange={e => setExact(e.target.checked)} />{t("exactSize")}</label><div className="my-8 flex justify-center rounded-xl bg-white p-5" dangerouslySetInnerHTML={{ __html: svg }} /><label className="block text-sm font-semibold text-[#2D4D35]">{t("rawSvg")}<textarea readOnly value={svg} rows={6} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white p-3 font-mono text-xs" /></label><label className="mt-4 block text-sm font-semibold text-[#2D4D35]">{t("dataUri")}<textarea readOnly value={dataUri} rows={4} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white p-3 font-mono text-xs" /></label><div className="mt-5 flex flex-wrap justify-center gap-3"><button onClick={() => copy(svg)} className="rounded-xl bg-gradient-indigo-cyan px-5 py-3 font-semibold text-white">{t("copySvg")}</button><button onClick={() => copy(dataUri)} className="rounded-xl border border-[#10A968] px-5 py-3 font-semibold text-[#10A968]">{t("copyDataUri")}</button><button onClick={download} className="rounded-xl border border-[#E0E0E0] px-5 py-3 font-semibold text-[#2D4D35]">{t("download")}</button></div></section></div>

      {/* Frequently Asked Questions */}
      {faqs && faqs.length > 0 && (
        <FaqSection items={faqs} title="Frequently Asked Questions" />
      )}
    </main>; }
