"use client";

import { useMemo, useState } from "react";
import { Copy } from "lucide-react";
import SEO from "@/components/SEO";
import { convertList, type ListSort } from "@/lib/converter-tools-3";
import { useTranslations } from "next-intl";
import FaqSection from "@/components/FaqSection";
import { getFaqsForRoute } from "@/lib/faq-data";

export default function ListConverter() { const t = useTranslations("Tools.ListConverter");
  const faqs = getFaqsForRoute("list-converter"); const [input, setInput] = useState("  Banana\nApple\nApple\n42\n  Pear  "); const [trim, setTrim] = useState(true); const [unique, setUnique] = useState(true); const [sort, setSort] = useState<ListSort>("alphabetic"); const [reverse, setReverse] = useState(false); const [prefix, setPrefix] = useState(""); const [suffix, setSuffix] = useState(""); const [separator, setSeparator] = useState("\n"); const output = useMemo(() => convertList(input, { trim, unique, sort, reverse, prefix, suffix, separator, listPrefix: "", listSuffix: "" }), [input, prefix, reverse, separator, sort, suffix, trim, unique]); return <main className="min-h-screen px-6 pb-20 pt-32 text-foreground"><SEO title={t("title")} description={t("description")} keywords="list converter sort deduplicate prefix suffix" /><div className="mx-auto max-w-6xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold lg:text-5xl">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><div className="grid gap-6 lg:grid-cols-2"><label className="text-sm font-semibold text-[#2D4D35]">{t("input")}<textarea value={input} onChange={(event) => setInput(event.target.value)} rows={16} className="mt-3 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 font-mono text-sm font-normal" /></label><div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-[#2D4D35]">{t("sort")}<select value={sort} onChange={(event) => setSort(event.target.value as ListSort)} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white px-3 py-2 font-normal"><option value="none">{t("none")}</option><option value="alphabetic">{t("alphabetic")}</option><option value="numeric">{t("numeric")}</option><option value="length">{t("length")}</option></select></label><label className="text-sm font-semibold text-[#2D4D35]">{t("separator")}<select value={separator} onChange={(event) => setSeparator(event.target.value)} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white px-3 py-2 font-normal"><option value="\n">{t("newline")}</option><option value=", ">{t("comma")}</option><option value="; ">{t("semicolon")}</option><option value=" | ">{t("custom")}</option></select></label></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-[#2D4D35]"><input type="checkbox" checked={trim} onChange={(event) => setTrim(event.target.checked)} className="mr-2" />{t("trim")}</label><label className="text-sm text-[#2D4D35]"><input type="checkbox" checked={unique} onChange={(event) => setUnique(event.target.checked)} className="mr-2" />{t("unique")}</label><label className="text-sm text-[#2D4D35]"><input type="checkbox" checked={reverse} onChange={(event) => setReverse(event.target.checked)} className="mr-2" />{t("reverse")}</label></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-[#2D4D35]">{t("prefix")}<input value={prefix} onChange={(event) => setPrefix(event.target.value)} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white px-3 py-2 font-normal" /></label><label className="text-sm font-semibold text-[#2D4D35]">{t("suffix")}<input value={suffix} onChange={(event) => setSuffix(event.target.value)} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white px-3 py-2 font-normal" /></label></div><div className="mt-5"><div className="flex items-center justify-between text-sm font-semibold text-[#2D4D35]"><span>{t("output")}</span><button type="button" onClick={() => void navigator.clipboard.writeText(output)} className="inline-flex items-center gap-2 rounded-lg px-3 py-2"><Copy size={16} />{t("copy")}</button></div><pre className="mt-2 max-h-72 overflow-auto whitespace-pre-wrap rounded-xl bg-[#18281D] p-4 font-mono text-sm leading-7 text-[#DDF5DF]">{output}</pre></div></div></div></section></div>

      {/* Frequently Asked Questions */}
      {faqs && faqs.length > 0 && (
        <FaqSection items={faqs} title="Frequently Asked Questions" />
      )}
    </main>; }
