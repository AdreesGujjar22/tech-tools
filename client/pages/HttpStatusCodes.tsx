"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { httpStatuses } from "@/lib/web-sec-3";
import { useTranslations } from "next-intl";
const categories = ["All", "Informational", "Success", "Redirection", "Client Error", "Server Error"];
export default function HttpStatusCodes() { const t = useTranslations("Tools.HttpStatusCodes"); const [search, setSearch] = useState(""); const [category, setCategory] = useState("All"); const results = useMemo(() => httpStatuses.filter(item => (category === "All" || item.category === category) && `${item.code} ${item.name} ${item.description}`.toLowerCase().includes(search.toLowerCase())), [category, search]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="HTTP status code directory reference" /><div className="mx-auto max-w-6xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold lg:text-5xl">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-6"><div className="grid gap-4 md:grid-cols-[1fr_280px]"><input value={search} onChange={e => setSearch(e.target.value)} placeholder={t("search")} className="rounded-xl border border-[#E0E0E0] bg-white px-4 py-3 text-[#2D4D35]" /><select value={category} onChange={e => setCategory(e.target.value)} className="rounded-xl border border-[#E0E0E0] bg-white px-4 py-3 text-[#2D4D35]">{categories.map(value => <option value={value} key={value}>{value === "All" ? t("all") : t(value.replace(" ", "").toLowerCase())}</option>)}</select></div><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead><tr className="border-b border-[#E0E0E0] text-sm text-[#4A6857]"><th className="p-4">{t("code")}</th><th className="p-4">{t("name")}</th><th className="p-4">{t("category")}</th><th className="p-4">{t("descriptionLabel")}</th></tr></thead><tbody>{results.map(item => <tr className="border-b border-[#E0E0E0] text-sm text-[#2D4D35]" key={item.code}><td className="p-4 font-mono font-bold">{item.code}</td><td className="p-4 font-semibold">{item.name}</td><td className="p-4">{item.category}</td><td className="p-4">{item.description}</td></tr>)}</tbody></table><p className="mt-4 text-sm text-[#4A6857]">{results.length} {t("results")}</p></div></section></div></main>; }
