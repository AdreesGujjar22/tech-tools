"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { colorToCmyk, colorToHex, colorToHsl, colorToHsv, colorToRgb, parseColor, type Color } from "@/lib/media-visual-tools";
import { useTranslations } from "next-intl";
export default function ColorConverter() { const t = useTranslations("Tools.ColorConverter"); const [format, setFormat] = useState<"hex" | "rgb" | "hsl" | "hsv" | "cmyk">("hex"); const [value, setValue] = useState("#1ea54c"); const color = useMemo(() => parseColor(value, format), [format, value]); const output = color ? { hex: colorToHex(color), rgb: colorToRgb(color), hsl: colorToHsl(color), hsv: colorToHsv(color), cmyk: colorToCmyk(color) } : null; const updatePicker = (hex: string) => { setFormat("hex"); setValue(hex); }; return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="color converter HEX RGB HSL HSV CMYK" /><div className="mx-auto max-w-4xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><div className="grid gap-5 sm:grid-cols-[140px_1fr_80px] sm:items-end"><label className="text-sm font-semibold text-[#2D4D35]">{t("format")}<select value={format} onChange={e => setFormat(e.target.value as typeof format)} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2"><option value="hex">HEX</option><option value="rgb">RGB</option><option value="hsl">HSL</option><option value="hsv">HSV</option><option value="cmyk">CMYK</option></select></label><label className="text-sm font-semibold text-[#2D4D35]">{t("value")}<input value={value} onChange={e => setValue(e.target.value)} placeholder={t(format)} className="mt-2 w-full rounded-lg border border-[#E0E0E0] bg-white px-3 py-2 font-mono" /></label><input type="color" value={output?.hex.slice(0, 7) || "#000000"} onChange={e => updatePicker(e.target.value)} className="h-10 w-full" /></div>{!color && <p className="mt-4 text-sm text-red-600">{t("invalid")}</p>}<div className="mt-8 grid gap-3 sm:grid-cols-2">{output && Object.entries(output).map(([key, converted]) => <div className="flex items-center justify-between rounded-lg bg-white px-4 py-3" key={key}><span className="font-semibold uppercase text-[#4A6857]">{key}</span><span className="font-mono text-sm text-[#2D4D35]">{converted}</span></div>)}</div></section></div></main>; }
