"use client";
import { useMemo, useState } from "react";
import { Copy } from "lucide-react";
import SEO from "@/components/SEO";
import { parsePhone } from "@/lib/text-data-2";
import { useTranslations } from "next-intl";
export default function PhoneParser() { const t = useTranslations("Tools.PhoneParser"); const [input, setInput] = useState("+33 1 42 68 53 00"); const result = useMemo(() => parsePhone(input), [input]); const rows = [[t("countryCode"), result.countryCode], [t("country"), result.country], [t("nationalPrefix"), result.nationalPrefix], [t("international"), result.international], [t("national"), result.national], [t("e164"), result.e164], [t("rfc3966"), result.rfc3966]]; return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="phone number parser E.164 formatter" /><div className="mx-auto max-w-4xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold lg:text-5xl">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><section className="glass-card-dark rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("input")}<input value={input} onChange={e => setInput(e.target.value)} placeholder="+14155552671" className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white px-4 py-3 font-mono text-[#2D4D35]" /></label><div className={`mt-5 rounded-xl p-4 text-sm font-semibold ${result.valid ? "bg-green-50 text-green-700" : "bg-red-50 text-red-700"}`}>{result.valid ? t("valid") : result.error || t("invalid")}</div><div className="mt-6 divide-y divide-[#E0E0E0] rounded-xl border border-[#E0E0E0] bg-white">{rows.map(([label, value]) => <div className="flex flex-col gap-1 p-4 sm:flex-row sm:justify-between" key={label as string}><span className="text-sm text-[#4A6857]">{label}</span><span className="break-all font-mono text-sm font-semibold text-[#2D4D35]">{value || "—"}</span>{value && <button onClick={() => void navigator.clipboard.writeText(value)} aria-label={t("copy")} className="self-start text-[#2D4D35] sm:self-auto"><Copy className="h-4 w-4" /></button>}</div>)}</div></section></div></main>; }
