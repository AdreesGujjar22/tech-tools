"use client";
import { useMemo, useState } from "react";
import SEO from "@/components/SEO";
import { getPasswordStrength } from "@/lib/crypto-tools";
import { useTranslations } from "next-intl";
export default function PasswordStrengthAnalyser() { const t = useTranslations("Tools.PasswordStrengthAnalyser"); const [password, setPassword] = useState(""); const result = useMemo(() => getPasswordStrength(password), [password]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="password strength, password crack time" /><div className="mx-auto max-w-3xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><div className="space-y-4"><input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder={t("placeholder")} className="w-full rounded-xl border border-[#E0E0E0] bg-white px-4 py-3 text-[#2D4D35] shadow-sm" /><section className="glass-card-dark rounded-[24px] p-8 text-center"><p className="text-sm text-[#4A6857]">{t("duration")}</p><p className="mt-2 text-2xl font-bold text-[#2D4D35]">{result.crackDuration}</p></section><section className="glass-card-dark space-y-3 rounded-[24px] p-8">{[[t("passwordLength"), result.passwordLength], [t("entropy"), Math.round(result.entropy * 100) / 100], [t("charsetLength"), result.charsetLength], [t("score"), `${Math.round(result.score * 100)} / 100`]].map(([label, value]) => <div className="flex gap-3" key={label}><span className="flex-1 text-right text-[#4A6857]">{label}</span><span className="flex-1 text-left text-[#2D4D35]">{value}</span></div>)}</section><p className="text-sm text-[#4A6857]"><strong>{t("noteLabel")}</strong> {t("note")}</p></div></div></main>; }
