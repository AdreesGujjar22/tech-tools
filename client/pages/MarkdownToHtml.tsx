"use client";
import { useMemo, useState } from "react";
import { Copy } from "lucide-react";
import SEO from "@/components/SEO";
import { markdownToHtml, sanitizeHtml } from "@/lib/dev-tools-3";
import { useTranslations } from "next-intl";
export default function MarkdownToHtml() { const t = useTranslations("Tools.MarkdownToHtml"); const [markdown, setMarkdown] = useState("# Hello\n\nThis is **bold** and *italic*.\n\n- One\n- Two\n\n[Visit the site](https://example.com)"); const html = useMemo(() => sanitizeHtml(markdownToHtml(markdown)), [markdown]); return <main className="min-h-screen px-6 pt-32 pb-20 text-foreground"><SEO title={t("title")} description={t("description")} keywords="Markdown HTML converter preview" /><div className="mx-auto max-w-6xl"><header className="mb-10 text-center"><h1 className="gradient-text text-4xl font-bold lg:text-5xl">{t("title")}</h1><p className="mt-3 text-[#4A6857]">{t("description")}</p></header><div className="grid gap-6 lg:grid-cols-2"><section className="glass-card-dark rounded-[24px] p-8"><label className="block text-sm font-semibold text-[#2D4D35]">{t("markdown")}<textarea value={markdown} onChange={e => setMarkdown(e.target.value)} rows={18} spellCheck={false} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-white p-4 font-mono text-sm text-[#2D4D35]" /></label></section><section className="glass-card-dark rounded-[24px] p-8"><h2 className="text-sm font-semibold text-[#2D4D35]">{t("preview")}</h2><div className="prose mt-2 min-h-[300px] max-w-none rounded-xl bg-white p-5 text-[#2D4D35]" dangerouslySetInnerHTML={{ __html: html }} /><div className="mt-5"><p className="text-sm font-semibold text-[#2D4D35]">{t("html")}</p><textarea readOnly value={html} rows={8} className="mt-2 w-full rounded-xl border border-[#E0E0E0] bg-[#F4F7F4] p-4 font-mono text-xs text-[#2D4D35]" /><button onClick={() => void navigator.clipboard.writeText(html)} className="mt-3 inline-flex items-center gap-2 rounded-xl bg-[#2D4D35] px-4 py-2 text-sm font-semibold text-white"><Copy className="h-4 w-4" />{t("copy")}</button></div></section></div></div></main>; }
