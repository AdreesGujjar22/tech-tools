export type TemperatureUnit = "C" | "F" | "K" | "R" | "De";
export function convertTemperature(value: number, from: TemperatureUnit, to: TemperatureUnit) { const kelvin = from === "C" ? value + 273.15 : from === "F" ? (value + 459.67) * 5 / 9 : from === "R" ? value * 5 / 9 : from === "De" ? 373.15 - value * 2 / 3 : value; return to === "C" ? kelvin - 273.15 : to === "F" ? kelvin * 9 / 5 - 459.67 : to === "R" ? kelvin * 9 / 5 : to === "De" ? (373.15 - kelvin) * 3 / 2 : kelvin; }
export function encodeBase64Utf8(value: string, urlSafe: boolean) { const bytes = new TextEncoder().encode(value); let binary = ""; bytes.forEach((byte) => { binary += String.fromCharCode(byte); }); const encoded = btoa(binary); return urlSafe ? encoded.replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "") : encoded; }
export function decodeBase64Utf8(value: string, urlSafe: boolean) { const normalized = value.trim().replace(/-/g, "+").replace(/_/g, "/"); const padded = normalized + "=".repeat((4 - normalized.length % 4) % 4); if (!/^[A-Za-z0-9+/]*={0,2}$/.test(padded)) throw new Error("Invalid Base64 string"); const binary = atob(padded); return new TextDecoder("utf-8", { fatal: true }).decode(Uint8Array.from(binary, (char) => char.charCodeAt(0))); }
export function formatStopwatch(ms: number) { const total = Math.floor(ms / 1000); const hours = Math.floor(total / 3600); const minutes = Math.floor((total % 3600) / 60); const seconds = total % 60; const centiseconds = Math.floor((ms % 1000) / 10); return `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}.${String(centiseconds).padStart(2, "0")}`; }
